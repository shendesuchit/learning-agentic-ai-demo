# Predefined Middleware

Predefined middleware represents reusable behavior supplied by a framework rather than written from scratch for every project.

## Dummy example

```python
enabled = True

if enabled:
    print("Use predefined middleware")
```

## Discussion

A useful question is:

> When should I use framework-provided middleware instead of writing custom behavior?
